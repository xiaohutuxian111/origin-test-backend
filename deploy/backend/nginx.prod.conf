server {
    listen 80;
    listen [::]:80;
    server_name _;

    client_max_body_size 20M;
    client_body_buffer_size 20M;

    gzip on;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/javascript application/javascript application/json application/xml+rss application/xml+atom image/svg+xml font/otf font/ttf font/opentype;
    gzip_min_length 256;
    gzip_vary on;

    keepalive_timeout 300;
    access_log /var/log/nginx/access.log;

    location / {
        proxy_pass http://fba_server:8001;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    location /flower/ {
        proxy_pass http://fba_celery_flower:8555;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        proxy_redirect off;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    location /static/upload {
        alias /www/fba_server/backend/upload;
    }

    location /static {
        alias /www/fba_server/backend/static;
    }
}

# Enable this server block after uploading certificates to ./ssl and replacing example.com.
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name example.com;
#
#     ssl_certificate /etc/nginx/ssl/example.com.pem;
#     ssl_certificate_key /etc/nginx/ssl/example.com.key;
#     ssl_session_timeout 10m;
#     ssl_protocols TLSv1.2 TLSv1.3;
#
#     location / {
#         proxy_pass http://fba_server:8001;
#         proxy_set_header Host $http_host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#     }
# }
